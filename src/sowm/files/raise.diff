diff --git a/sowm.c b/sowm.c
index 509e28f..8ddf5d3 100644
--- a/sowm.c
+++ b/sowm.c
@@ -33,6 +33,7 @@ static void (*events[LASTEvent])(XEvent *e) = {
 
 void win_focus(client *c) {
     cur = c;
+    XRaiseWindow(d, cur->w);
     XSetInputFocus(d, cur->w, RevertToParent, CurrentTime);
 }
 
